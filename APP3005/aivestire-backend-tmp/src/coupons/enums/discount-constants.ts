export enum DiscountType {
    PERCENTAGE = 'PERCENTAGE',
    FIXED = 'FIXED',
}

export const MAX_PERCENTAGE_DISCOUNT = 100;

export enum CouponStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    EXPIRED = 'EXPIRED',
}

export const MIN_ORDER_AMOUNT = 0;

export const MAX_USAGE = 1000000;

export const MIN_USAGE = 1;

