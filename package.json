{
    "name": "aicore-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "frontend": "npm run frontend --prefix APP3005",
        "backend": "npm run backend --prefix APP3005",
        "dev": "npm run dev --prefix APP3005",
        "dev:payu-test": "docker compose --env-file .env.payu-test.local -f docker-compose.yml -f docker-compose.payu-test.override.yml up -d --force-recreate backend backend-worker frontend",
        "dev:payu-test:logs": "docker compose --env-file .env.payu-test.local -f docker-compose.yml -f docker-compose.payu-test.override.yml logs --tail=50 backend",
        "dev:payu-test:down": "docker compose --env-file .env.payu-test.local -f docker-compose.yml -f docker-compose.payu-test.override.yml down",
        "start": "npm run start --prefix APP3005",
        "worker": "npm run start:worker --prefix APP3005/aivestire-backend-tmp",
        "db:generate": "npm run db:generate --prefix APP3005",
        "db:migrate": "npm run db:migrate --prefix APP3005"
    }
}
