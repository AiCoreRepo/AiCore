# infra/k8s/base/secrets/redis-sealed-secret.yaml
# ──────────────────────────────────────────────────────────────────────────────
# PLACEHOLDER — contains NO real data. Do not apply this file as-is.
#
# Real secret kept OUT of git. Generate + apply by hand (see DEPLOYMENT.md §5b):
#
#   kubectl create secret generic redis-secret \
#     --from-literal=REDIS_PASSWORD="<strong-pw>" \
#     --namespace aivestire --dry-run=client -o yaml \
#     | kubeseal --format yaml --namespace aivestire \
#     > ~/aivestire-sealed-secrets-private/redis-sealed-secret.yaml
#   kubectl apply -f ~/aivestire-sealed-secrets-private/redis-sealed-secret.yaml
# ──────────────────────────────────────────────────────────────────────────────
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: redis-secret
  namespace: aivestire
spec:
  encryptedData:
    REDIS_PASSWORD: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    metadata:
      name: redis-secret
      namespace: aivestire
