services:
  fusion_api:
    build:
      context: .
    command: ["uvicorn", "app.fusion_api:app", "--host", "0.0.0.0", "--port", "8791"]
    ports:
      - "0.0.0.0:8791:8791"
    volumes:
      - ./artifacts:/app/artifacts:ro
      - ./collection.json:/app/collection.json:ro
