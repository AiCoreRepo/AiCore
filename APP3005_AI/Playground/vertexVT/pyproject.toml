[project]
name = "vertex-virtual-tryon"
version = "0.1.0"
description = "Streamlit UI to call the Vertex AI virtual try-on model."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.27.0",
    "requests>=2.31.0",
]

[dependency-groups]
dev = []
