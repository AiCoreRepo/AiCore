services:
  api:
    build: .
    container_name: vertex-vt-api
    ports:
      - "0.0.0.0:8000:8000"
    # Load all environment variables from .env file
    # DO NOT use 'environment' section as it overrides with shell env vars
    env_file:
      - .env
    restart: unless-stopped
