version: "3.9"

services:
  fashion-mlp:
    build: .
    image: fashion-mlp
    container_name: fashion-mlp
    ports:
      - "0.0.0.0:8509:8509"
    environment:
      PYTHONUNBUFFERED: 1
    command: streamlit run app.py --server.port=8509 --server.address=0.0.0.0
